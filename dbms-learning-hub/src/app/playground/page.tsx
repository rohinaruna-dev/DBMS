import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'DBMS Playground', description: 'Interactive DBMS Simulator - Insert, Update, Delete, Search student records in-browser' };
import PlaygroundPage from './PlaygroundClient';
export default PlaygroundPage;
